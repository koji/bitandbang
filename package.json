{
  "name": "@kojikanao/card",
  "version": "0.1.1",
  "description": "A personal card for Koji Kanao (@koji)",
  "main": "./bin/card.js",
  "bin": {
    "koji": "./bin/card.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/koji/bitandbang"
  },
  "engines": {
    "node": ">=8"
  },
  "scripts": {
    "prepare": "npm run build",
    "build": "node build.js",
    "dev": "npm run build && node ./bin/card.js",
    "lint": "standard",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "card",
    "npm",
    "npm card",
    "npx",
    "npx card",
    "business card"
  ],
  "author": "koji kanao",
  "license": "MIT",
  "files": [
    "bin/card.js",
    "bin/output"
  ],
  "devDependencies": {
    "boxen": "^2.1.0",
    "chalk": "^2.4.1",
    "standard": "^12.0.1"
  },
  "dependencies": {

  }
}
